using X.Proto.Db.Component.MasterDetails;

namespace TestOfClaudeGen.Core.User.Jobs.Posts
{
    /// <summary>
    /// UML: Core.User.Job.Posts page — «interface» IUserJobPosts : IXMasterDetail
    /// Reached from Core.User.Jobs via an &lt;&lt;include&gt;&gt; edge on the UseCases
    /// page, so it lives as a subfolder of Core.User.Jobs, not its own project.
    /// </summary>
    public interface IUserJobPosts : IXMasterDetail
    {
    }
}
